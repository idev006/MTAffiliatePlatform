# Program 1 Process Conformance CI — Round 2

Round 1 CAPA:
- background CommonJS test harness updated for injected lifecycle module;
- semantic workflow anchors replace brittle exact-string conformance check.

Acceptance: Ruff + Program 1 conformance + core/contract coverage + SQLite/Alembic + stress + extension build/tests must all PASS.